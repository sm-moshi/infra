# Deploy Prometheus node_exporter on non-Kubernetes hosts (systemd-managed)
- name: "Node Exporter | Deploy"
  hosts: "proxmox:k3s_control_plane:k3s_workers"
  become: true
  gather_facts: true

  roles:
    - role: node_exporter

  tags:
    - node_exporter
    - prometheus
    - monitoring
