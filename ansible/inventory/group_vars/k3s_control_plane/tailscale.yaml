tailscale_authkey: "{{ tailscale_authkey_k3s_control }}"
tailscale_force_reauth: true
tailscale_enable_auto_up: true
tailscale_up_extra_args: >
  --ssh=true --accept-dns=false --accept-routes=false --advertise-tags=tag:k8s,tag:k8s-operator
