---
# Bastion — /etc/hosts

- name: Configure /etc/hosts
  ansible.builtin.template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: "0644"
  when: base_hosts_entries | default([]) | length > 0
