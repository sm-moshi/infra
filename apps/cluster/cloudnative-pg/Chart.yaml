apiVersion: v2
name: cloudnative-pg
description: Wrapper chart for CloudNativePG operator (DHI) + shared cluster (apps namespace) + Barman Cloud plugin
icon: https://cloudnative-pg.io/favicon.ico
type: application

# Wrapper chart version (bump when you change templates/values)
version: 0.6.11
appVersion: "1.27.3"
dependencies:
  - name: cloudnative-pg-chart
    alias: cloudnative-pg
    version: 0.27.1
    repository: oci://dhi.io
  # Barman Cloud CNPG-I plugin chart (stays on upstream)
  - name: plugin-barman-cloud
    version: 0.5.0
    repository: https://cloudnative-pg.github.io/charts
