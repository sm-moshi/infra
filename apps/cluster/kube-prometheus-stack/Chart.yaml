apiVersion: v2
name: kube-prometheus-stack
description: Wrapper chart to deploy kube-prometheus-stack with pinned upstream dependency
version: 0.1.3
appVersion: v0.88.1
type: application
icon: https://raw.githubusercontent.com/prometheus/prometheus/main/documentation/images/prometheus-logo.svg
dependencies:
  # NOTE: Before upgrading, ensure Prometheus Operator CRDs are updated separately
  # (manage CRDs via a dedicated app/manifest before syncing this chart).
  - name: kube-prometheus-stack
    version: 81.4.2
    repository: https://prometheus-community.github.io/helm-charts
