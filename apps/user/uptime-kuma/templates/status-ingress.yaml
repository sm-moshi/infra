{{- if .Values.statusIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-status
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: status
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Release.Namespace }}-{{ .Release.Name }}-status-ratelimit@kubernetescrd
    external-dns.alpha.kubernetes.io/ignore: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - {{ .Values.statusIngress.host }}
      secretName: wildcard-m0sh1-cc
  rules:
    - host: {{ .Values.statusIngress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: 3001
{{- end }}
