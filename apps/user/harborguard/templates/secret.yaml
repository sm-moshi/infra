{{- if and (not .Values.database.existingSecret.name) .Values.database.url }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "harborguard.fullname" . }}-env
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.database.url | quote }}
{{- end }}
