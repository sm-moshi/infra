apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "harborguard.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "harborguard.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "harborguard.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "harborguard.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
    spec:
      serviceAccountName: {{ include "harborguard.serviceAccountName" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: harborguard
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          env:
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            {{- if .Values.database.existingSecret.name }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.existingSecret.name }}
                  key: {{ .Values.database.existingSecret.key }}
            {{- else if .Values.database.url }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "harborguard.fullname" . }}-env
                  key: DATABASE_URL
            {{- end }}
            {{- if and .Values.harbor.enabled .Values.harbor.credentials.existingSecret }}
            - name: HARBOR_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.harbor.credentials.existingSecret }}
                  key: {{ .Values.harbor.credentials.usernameKey }}
            - name: HARBOR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.harbor.credentials.existingSecret }}
                  key: {{ .Values.harbor.credentials.passwordKey }}
            {{- end }}
            - name: DOCKER_HOST
              value: "unix:///var/run/docker.sock"
          securityContext:
            privileged: {{ ternary true .Values.securityContext.privileged .Values.autopatch.enabled }}
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.dockerSocket.enabled }}
            - name: docker-sock
              mountPath: {{ .Values.dockerSocket.mountPath }}
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - name: workspace
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
        {{- if .Values.dind.enabled }}
        - name: dind
          image: "{{ .Values.dind.image.repository }}:{{ .Values.dind.image.tag }}"
          securityContext:
            privileged: true
          resources:
            {{- toYaml .Values.dind.resources | nindent 12 }}
          volumeMounts:
            - name: docker-sock
              mountPath: /var/run
            - name: workspace
              mountPath: /workspace
        {{- end }}
      volumes:
        {{- if .Values.dockerSocket.enabled }}
        - name: docker-sock
          emptyDir: {}
        {{- end }}
        {{- if .Values.persistence.enabled }}
        - name: workspace
          persistentVolumeClaim:
            claimName: {{ include "harborguard.fullname" . }}-data
        {{- end }}
