{{- if .Values.tika.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tika.fullname" . }}
  labels:
    {{- include "tika.labels" . | nindent 4 }}
spec:
  type: {{ .Values.tika.service.type }}
  ipFamilyPolicy: {{ .Values.tika.service.ipFamilyPolicy | default "SingleStack" }}
  selector:
    {{- include "tika.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.tika.service.port }}
      targetPort: http
{{- end }}
