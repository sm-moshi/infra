kubescape-operator:
  clusterName: lab
  ksNamespace: kubescape
  global:
    overrideRuntimePath: "/host/var/lib/rancher/k3s/data/current/bin/runc"
  capabilities:
    # Functional baseline:
    # - enable operator + posture scanning + storage API backend
    # - keep vulnerability/node/runtime features off for now
    #
    # Chart behavior note: for this chart version, `backend-storage: disable`
    # is required for in-cluster storage/APIService components to be rendered.
    backend-storage: disable
    operator: enable
    configurationScan: enable
    continuousScan: enable
    networkEventsStreaming: disable
    nodeScan: disable
    nodeSbomGeneration: disable
    vulnerabilityScan: disable
    relevancy: disable
    runtimeObservability: disable
    runtimeDetection: disable
    malwareDetection: disable
    networkPolicyService: disable
    nodeProfileService: disable
    admissionController: disable
    httpDetection: disable
    seccompProfileService: disable
    autoUpgrading: disable
    prometheusExporter: disable

  storage:
    mtls:
      enabled: false
  kubescape:
    resources:
      requests:
        cpu: 250m
        memory: 256Mi
      limits:
        cpu: 750m
        memory: 512Mi

  # kubevuln defaults to 5Gi memory limit; that is unnecessary for our current
  # workload and can contribute to node pressure. Keep enough headroom for
  # image scans, but cap it below the upstream default.
  kubevuln:
    resources:
      requests:
        cpu: 300m
        memory: 1Gi
        ephemeral-storage: 5Gi
      limits:
        cpu: 1500m
        memory: 2Gi
        ephemeral-storage: 10Gi
  configurations:
    priorityClass:
      enabled: false
  operator:
    # Explicitly size the controller deployment (this is the chart key used by
    # the rendered `operator` Deployment).
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
