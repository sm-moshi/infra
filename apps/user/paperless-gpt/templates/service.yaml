apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperless-gpt.fullname" . }}
  labels:
    {{- include "paperless-gpt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ipFamilyPolicy: {{ .Values.service.ipFamilyPolicy | default "SingleStack" }}
  selector:
    {{- include "paperless-gpt.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
