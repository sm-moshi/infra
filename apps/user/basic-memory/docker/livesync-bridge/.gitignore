# Upstream source (clone with --recurse-submodules to populate)
lib/
*.ts
*.lock
*.sh
docker-compose.yml
readme.md
script/
dat/*
vault*/*
*.log
node_modules

# Keep build essentials tracked
!.gitignore
!.dockerignore
