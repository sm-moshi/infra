apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "basic-memory.fullname" . }}-config
  labels:
    {{- include "basic-memory.labels" . | nindent 4 }}
data:
  config.json: |
    {
      "default_project_mode": true,
      "default_project": "main"
    }
