apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "proxmenux.fullname" . }}-forwardauth
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "proxmenux.labels" . | nindent 4 }}
spec:
  forwardAuth:
    address: {{ .Values.forwardAuth.address | quote }}
    trustForwardHeader: {{ .Values.forwardAuth.trustForwardHeader }}
    maxResponseBodySize: 4096
    authResponseHeaders:
      {{- range .Values.forwardAuth.authResponseHeaders }}
      - {{ . | quote }}
      {{- end }}
