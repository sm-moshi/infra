apiVersion: v1
kind: ResourceQuota
metadata:
  name: observability-quota
  namespace: observability
spec:
  hard:
    requests.cpu: "28"
    requests.memory: 40Gi
    limits.cpu: "40"
    limits.memory: 48Gi
