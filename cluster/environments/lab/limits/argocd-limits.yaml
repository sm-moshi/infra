apiVersion: v1
kind: LimitRange
metadata:
  name: argocd-limitrange
  namespace: argocd
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: 1Gi
      defaultRequest:
        cpu: 200m
        memory: 512Mi
