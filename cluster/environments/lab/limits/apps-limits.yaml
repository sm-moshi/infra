apiVersion: v1
kind: LimitRange
metadata:
  name: apps-limitrange
  namespace: apps
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: 2Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
